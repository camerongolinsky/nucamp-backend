// will render a list of the product components

import React from "react";


const ProductList = (props) => {
  console.log(props.trash);
  return <div></div>;
};
// loop through props.products using map
// render the product component for each product that exists in props.products
export default ProductList;
